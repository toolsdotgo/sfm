v1.2.0

- Now deploys S3 hosted templates directly from S3, increasing max template size to 1MB
- No longer need to specify the region flag (`-r`) or env var, can use the AWS SDK configured region instead
- Various dependency updates

v1.1.1

- Now with 100% more authentic Darwin ARM64 binaries! - Thanks to @samjarrett
- Various dependency updates

v1.1.0

- Change default behaviour to wait and print events on `mk` and `rm`
- Added `-nowait` flag
- Various dependency updates

v1.0.0

- Initial public release
